server {
    listen 80;
    server_name opentokenhub.fun www.opentokenhub.fun;

    root /var/www/aigc-rewriter;
    index index.html;

    # gzip 压缩
    gzip on;
    gzip_types text/html text/css application/javascript;
    gzip_min_length 256;

    # 静态资源缓存
    location ~* \.(css|js|pdf|png|jpg|svg|ico)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
